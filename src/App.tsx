import { Input } from "components/Input/Input";

function App() {
    return (
        <div>
            <Input
                placeholder="Enter your name"
                value=""
                onChange={() => ""}
            />
        </div>
    );
}

export default App;
